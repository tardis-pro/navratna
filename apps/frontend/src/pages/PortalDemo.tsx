import React from 'react';
import { PortalWorkspace } from '../components/futuristic/PortalWorkspace';

export const PortalDemo: React.FC = () => {
  return (
    <div className="w-full h-screen">
      {/* Live Portal System with Real Data Integration */}
      <PortalWorkspace />
    </div>
  );
}; 