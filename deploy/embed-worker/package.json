{
  "name": "@navratna/embed-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Embeddings worker — generates embeddings via Cloudflare Workers AI (@cf/baai/bge-large-en-v1.5, 1024-dim) and returns them in an OpenAI-compatible shape.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:production": "wrangler deploy --env production",
    "dry-run": "wrangler deploy --dry-run",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240117.0",
    "typescript": "^5.3.0",
    "wrangler": "^3.24.0"
  }
}
