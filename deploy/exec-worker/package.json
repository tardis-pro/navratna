{
  "name": "@navratna/exec-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Hybrid Execution Mesh — Cloudflare exec-worker (light tier). Runs pure-JS tools + http/streamable-http MCP proxy. See docs/specs/11-HYBRID-EXECUTION-MESH.md §3.1.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:production": "wrangler deploy --env production",
    "dry-run": "wrangler deploy --dry-run",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240117.0",
    "typescript": "^5.3.0",
    "wrangler": "^3.24.0"
  }
}
